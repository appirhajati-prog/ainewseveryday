# 🚀 راهنمای قدم به قدم اجرا روی گیت‌هاب (رایگان، بدون سرور)

این راهنما به شما یاد می‌دهد چطور ربات هوش مصنوعی روزانه را روی گیت‌هاب اجرا کنید.

---

## قدم ۱: حساب گیت‌هاب بسازید

1. به آدرس **https://github.com** بروید
2. روی دکمه **Sign up** کلیک کنید
3. ایمیل، رمز عبور و یک نام کاربری انتخاب کنید
4. وارد حساب خود شوید

---

## قدم ۲: یک ریپازیتوری (محل پروژه) جدید بسازید

1. در صفحه اصلی گیت‌هاب، روی آیکون **+** بالا سمت راست کلیک کنید
2. **New repository** را انتخاب کنید
3. این تنظیمات را وارد کنید:
   - **Repository name:** `ainewseveryday`
   - **Description:** `AI News Daily Bot`
   - **Public** را انتخاب کنید
4. تیک **Add a README file** را بزنید
5. روی دکمه **Create repository** کلیک کنید

---

## قدم ۳: فایل‌های پروژه را آپلود کنید

### روش ساده (آپلود از مرورگر):

1. در صفحه ریپازیتوری جدید، روی **Add file** > **Upload files** کلیک کنید
2. تمام محتویات پوشه `ainewseveryday` را بکشید و در صفحه رها کنید
3. **⚠️ مهم:** فقط محتویات داخل پوشه را آپلود کنید، نه خود پوشه!
4. تیک **Commit directly to the main branch** را بزنید
5. روی **Commit changes** کلیک کنید

### ⚠️ نکته مهم:
فایل `.env` را **آپلود نکنید!** این فایل حاوی رمزهای شماست و باید محرمانه بماند.

---

## قدم ۴: رمزهای ربات تلگرام را به گیت‌هاب بدهید

### ۴.۱: توکن ربات تلگرام را بسازید:

1. در تلگرام، دنبال **@BotFather** بگردید و وارد چت شوید
2. دستور زیر را بفرستید:
   ```
   /newbot
   ```
3. یک اسم برای ربات انتخاب کنید (مثلاً: `AINewsDaily`)
4. یک یوزرنیم انتخاب کنید (مثلاً: `my_ai_news_bot`)
5. BotFather یک توکن به شما می‌دهد. چیزی شبیه این:
   ```
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
6. **این توکن را کپی کنید**

### ۴.۲: آیدی چت خود را پیدا کنید:

1. رباتی که ساختید را پیدا کنید و یک پیام برایش بفرستید (مثلاً `/start`)
2. به آدرس زیر در مرورگر بروید (توکن خودتان را جایگزین کنید):
   ```
   https://api.telegram.org/botYOUR_TOKEN/getUpdates
   ```
3. در نتیجه، دنبال `"chat":{"id":` بگردید — عددی که بعد از آن می‌آید **CHAT_ID** شماست

### ۴.۳: این مقادیر را در گیت‌هاب ذخیره کنید:

1. در صفحه ریپازیتوری خود، روی **Settings** کلیک کنید (تب بالا)
2. از منوی سمت چپ، **Secrets and variables** > **Actions** را انتخاب کنید
3. روی دکمه **New repository secret** کلیک کنید
4. اولین سcret:
   - **Name:** `TELEGRAM_TOKEN`
   - **Secret:** (توکنی که از BotFather گرفتید)
   - روی **Add secret** کلیک کنید
5. همین کار را برای سcret دوم تکرار کنید:
   - **Name:** `CHAT_ID`
   - **Secret:** (عدد آیدی چت شما)

---

## قدم ۵: اجرای خودکار را فعال کنید

1. در صفحه ریپازیتوری، روی تب **Actions** کلیک کنید
2. اگر پیامی دیدید که Actions را فعال کنید، روی **"I understand my workflows, go ahead and enable them"** کلیک کنید
3. در سمت چپ، **Daily AI News Digest** را ببینید و روی آن کلیک کنید
4. روی **Run workflow** کلیک کنید تا یک‌بار امتحان کنید
5. چند دقیقه صبر کنید و پیام‌های ربات را در تلگرام بررسی کنید!

**تمام! 🎉** از این به بعد ربات هر روز ساعت ۱۲:۰۰ ظهر به وقت ایران به‌صورت خودکار پیام هوش مصنوعی روزانه را در تلگرام برایتان ارسال می‌کند.

---

## 🔍 مشکل‌یابی

### اگر ربات پیام نفرستاد:
1. در تب **Actions** گیت‌هاب ببینید آیا اجرای اخیر سبز (موفق) است یا قرمز (ناموفق)
2. روی آخرین اجرا کلیک کنید و ببینید خطای چیست
3. رایج‌ترین مشکلات:
   - **TELEGRAM_TOKEN اشتباه است** → توکن را از BotFather چک کنید
   - **CHAT_ID اشتباه است** → ابتدا به ربات پیام بدهید، سپس getUpdates را چک کنید
   - **Secrets ذخیره نشده‌اند** → Settings > Secrets را بررسی کنید

### برای اجرای دستی:
هر وقت خواستید ربات را اجرا کنید، کافیست:
1. بروید به تب **Actions**
2. **Daily AI News Digest** را انتخاب کنید
3. **Run workflow** را بزنید